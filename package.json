{
  "name": "my-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "project-details.js",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./dist/output.css --watch",
    "start": "concurrently \"npm run build\" \"live-server --port=3000 --no-browser\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
